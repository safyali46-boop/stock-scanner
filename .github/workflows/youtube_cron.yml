name: YouTube to Scanner Auto Bot

on:
  schedule:
    # بيشتغل أوتوماتيك كل نص ساعة (تقدر تعدل الوقت لو عاوزه أسرع)
    - cron: '*/30 * * * *'
  workflow_dispatch: # زرار للتشغيل اليدوي وقت ما تحب من جيت هوب

jobs:
  run-bot:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install requests

      - name: Run YouTube processing script
        run: python youtube_scanner.py
